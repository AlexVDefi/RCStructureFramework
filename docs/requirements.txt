# Documentation toolchain (Sphinx + MyST Markdown + Furo theme).
# Used by the GitHub Pages workflow, and
# matches the versions the docs were authored/built against locally.
sphinx>=8,<10
myst-parser>=4,<6
furo>=2024.1
sphinx-copybutton>=0.5,<1
sphinx-design>=0.6,<1
